# 🎓 구글 스프레드시트 연동 학생 포트폴리오 갤러리 설정 가이드

구글 폼과 스프레드시트를 활용하여 학생 포트폴리오를 자동으로 관리하는 시스템입니다.

## 📋 시스템 구조

```
구글 폼 (학생 제출)
    ↓
구글 스프레드시트 (자동 저장)
    ↓
Google Apps Script (JSON API 변환)
    ↓
웹사이트 (자동 표시)
```

---

## 🚀 1단계: 구글 스프레드시트 준비

### 1-1. 새 스프레드시트 만들기
1. [Google Sheets](https://sheets.google.com) 접속
2. 빈 스프레드시트 만들기
3. 제목을 "학생 포트폴리오 데이터"로 변경

### 1-2. 헤더 행 작성
첫 번째 행(A1~E1)에 다음 헤더를 정확히 입력하세요:

| A열 | B열 | C열 | D열 | E열 |
|-----|-----|-----|-----|-----|
| 이름 | GitHub ID | 사이트 URL | 그룹 | 썸네일 |

**⚠️ 중요: 헤더 이름은 정확히 일치해야 합니다!**

### 1-3. 예시 데이터 입력

| 이름 | GitHub ID | 사이트 URL | 그룹 | 썸네일 |
|------|-----------|-----------|------|--------|
| 김지수 | dyktdesign | https://dyktdesign.github.io/ | 2024-1학기 | |
| 이민호 | minho-lee | | 2024-1학기 | |
| 박서연 | seoyeon-park | https://seoyeon-park.github.io/ | 2024-2학기 | |

**참고:**
- **사이트 URL이 비어있으면** GitHub ID로 자동 생성됩니다 (`https://아이디.github.io/`)
- **썸네일**은 선택사항입니다 (비워두면 이니셜 표시)

---

## 📝 2단계: 구글 폼 만들기

### 2-1. 새 폼 만들기
1. [Google Forms](https://forms.google.com) 접속
2. 빈 양식 만들기
3. 제목: "학생 포트폴리오 정보 제출"

### 2-2. 질문 추가

#### 질문 1: 이름 (필수)
- 유형: 단답형
- 질문: "이름을 입력하세요"
- 필수 설정

#### 질문 2: GitHub ID (필수)
- 유형: 단답형
- 질문: "GitHub 아이디를 입력하세요"
- 설명: "@는 제외하고 아이디만 입력하세요. 예: dyktdesign"
- 필수 설정

#### 질문 3: 사이트 URL (선택)
- 유형: 단답형
- 질문: "포트폴리오 사이트 URL (선택)"
- 설명: "비워두면 https://아이디.github.io/ 로 자동 설정됩니다"

#### 질문 4: 그룹 (필수)
- 유형: 객관식 또는 드롭다운
- 질문: "수강 과목/학기를 선택하세요"
- 선택지 예시:
  - 2024-1학기
  - 2024-2학기
  - 웹디자인 A반
  - 웹디자인 B반

### 2-3. 폼을 스프레드시트에 연결
1. 폼 우측 상단의 "응답" 탭 클릭
2. 스프레드시트 아이콘 클릭
3. "기존 스프레드시트 선택" → 1단계에서 만든 스프레드시트 선택
4. "선택" 클릭

**⚠️ 주의:** 폼 응답이 새 시트에 저장되므로, 해당 시트의 열 이름을 확인하고 필요시 조정하세요.

---

## ⚙️ 3단계: Google Apps Script 설정

### 3-1. Apps Script 열기
1. 스프레드시트 상단 메뉴: **확장 프로그램 > Apps Script**
2. 자동으로 코드 에디터가 열립니다

### 3-2. 코드 붙여넣기
1. 기존 코드를 모두 삭제
2. 제공된 `Code.gs` 파일의 내용을 복사하여 붙여넣기
3. 저장 버튼 클릭 (💾)

### 3-3. 웹 앱으로 배포
1. 우측 상단 **배포 > 새 배포** 클릭
2. 설정:
   - **유형 선택**: 웹 앱
   - **설명**: "학생 포트폴리오 API" (선택사항)
   - **다음 계정으로 실행**: 나
   - **액세스 권한**: 모든 사용자 ⚠️ **중요!**
3. **배포** 클릭
4. **액세스 승인** 클릭하여 권한 부여
5. **웹 앱 URL** 복사 (나중에 사용)

예시 URL: `https://script.google.com/macros/s/AKfycbx.../exec`

---

## 🌐 4단계: 웹사이트 설정

### 4-1. HTML 파일 수정
`student-gallery-sheets.html` 파일을 열어 다음 부분을 찾습니다:

```javascript
const SHEET_API_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
```

3-3단계에서 복사한 웹 앱 URL로 교체:

```javascript
const SHEET_API_URL = 'https://script.google.com/macros/s/AKfycbx.../exec';
```

### 4-2. GitHub Pages에 배포
```bash
git clone https://github.com/your-username/student-gallery.git
cd student-gallery

# 파일 추가
cp student-gallery-sheets.html index.html
git add index.html
git commit -m "구글 스프레드시트 연동 갤러리"
git push

# Settings > Pages에서 main 브랜치 선택하여 배포
```

---

## ✅ 5단계: 테스트

### 5-1. 스프레드시트 API 테스트
1. Apps Script 에디터에서 `testDoGet` 함수 선택
2. 실행 버튼 클릭
3. 로그에서 JSON 출력 확인

### 5-2. 웹 브라우저 테스트
웹 앱 URL을 브라우저에 직접 입력하여 JSON 데이터 확인

### 5-3. 갤러리 사이트 테스트
배포된 GitHub Pages URL에서 학생 카드 표시 확인

---

## 💡 사용 시나리오

### 시나리오 1: 학기 초
1. 구글 폼 링크를 학생들에게 공유
2. 학생들이 정보 제출
3. 스프레드시트에 자동 수집
4. 웹사이트에 자동 표시 ✨

### 시나리오 2: 여러 강의 관리
- 각 강의마다 "그룹" 값 설정
- 웹사이트에서 그룹별 필터링 가능
- 하나의 사이트로 모든 강의 관리 가능

---

## 🔧 문제 해결

### "데이터를 불러오는데 실패했습니다"
- [ ] 웹 앱 URL이 정확한지 확인
- [ ] Apps Script 배포 시 "액세스 권한: 모든 사용자" 선택했는지 확인
- [ ] 스프레드시트 헤더 이름이 정확한지 확인

### 데이터가 업데이트되지 않음
- 브라우저 캐시 삭제 (Ctrl + Shift + R)
- Apps Script를 새로 배포 (배포 > 배포 관리 > 새 버전)

---

## 🎉 완료!

이제 학생들의 포트폴리오를 편리하게 관리할 수 있습니다!
- ✅ 구글 폼으로 손쉬운 제출
- ✅ 스프레드시트로 간편한 관리
- ✅ 웹사이트에 자동 반영
- ✅ 그룹별 필터링으로 여러 강의 관리
